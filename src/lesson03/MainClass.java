package lesson03;



import java.util.Random;
import java.util.Scanner;

public class MainClass {

    public static void main(String[] args) {

      /*  Random random = new Random();

        System.out.println("1 - угадываем номер, 2 - угадываем слово");

        switch (getNumberFromConsole()) {
            case 1:
                guessNumber();
                break;
            case 2:
                guessTheWord();
                break;
        }


        Scanner scanner = new Scanner(System.in); // ввод числа c клавиатуры

        do {
            if (scanner.hasNextInt()) // hasNextInt проверяет , введено число или другое (пропускает числа)
        }
        return scanner.nextInt();

        System.out.print("Угадай число (0..9): ");
        scanner.nextInt();                // cтирает все после проверки

        while (true) ;*/

    }

}


